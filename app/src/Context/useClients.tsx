import React, { useCallback, useMemo } from 'react'

function useClients() {
    const saveClient = useCallback(async (): Promise<void> => {

    }, [])
    return useMemo(() => ({

    }), [

    ])
}

export default useClients
